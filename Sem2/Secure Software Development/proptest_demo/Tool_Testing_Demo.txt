test_1_numeric_dates

Type: Property-based test via proptest!.

Strategy: randomly picks

year ∈ 0..10 000

month ∈ 1..13 (i.e. 1–12)

day ∈ 1..32 (i.e. 1–31)

What it checks:

Formats them as "YYYY-MM-DD".

Computes expected = Some((y,m,d)) if d ≤ days_in_month(y,m), else None.

Asserts parse_date(date_str) == expected.

Logging: prints each candidate date before asserting.

Why: catches off-by-ones in month lengths, leap-year logic, etc., over 10 random samples.

test_2_garbage_strings

Type: Property-based test.

Strategy: s in "\\PC*" → arbitrary printable Unicode strings.

What it checks:

For each random s, asserts parse_date(s) == None.

Logging: prints the garbage string.

Why: ensures any malformed input is rejected, not mis-parsed.

test_3_doesnt_crash

Type: Property-based test.

Strategy: same \\PC* garbage-string generator.

What it checks:

Calls parse_date(s) but makes no assertion on the return value—only that it doesn’t panic.

Logging: prints each test string.

Why: guarantees full panic-safety even on extremely weird inputs.

test_4_demo_failing_case

Type: Manual unit test.

Input: "2025-04-31" (April 31).

What it checks:

Asserts parse_date("2025-04-31") == None.

Logging: prints the input and the returned Option.

Why: locks down that specific 30-day‐month edge which random tests might never hit.

test_month_out_of_range

Type: Manual unit test.

Input: "2025-13-01".

What it checks:

Asserts parse_date(...) == None.

Why: Proptest’s numeric strategy never generated month=13, so we explicitly cover that.

test_day_zero

Type: Manual unit test.

Input: "2025-01-00".

What it checks:

Asserts parse_date(...) == None.

Why: day=0 is another out-of-range value not generated by Proptest’s 1..32 strategy.

test_bad_format_positions

Type: Manual unit test.

Inputs:

"2025/02/28" (wrong separator)

"20250228" (missing hyphens)

"abcd-ef-gh" (non-digits)

What it checks:

Each yields None.

Why: ensures format checks (length, ASCII, hyphens at 4 & 7) are enforced.

test_leap_years

Type: Manual unit test.

Inputs & expected:

"2000-02-29" → Some((2000,2,29)) (400-year rule)

"1900-02-29" → None (century not divisible by 400)

"2021-02-29" → None (non-leap year)

Why: documents the exact leap-year behavior you implement in is_leap_year.

demo_numeric_dates_full_log

Type: Manual driver using Proptest’s low-level API.

Config: 20 cases, no shrinking.

Strategy:

year ∈ 0..10 000

month ∈ 0..15 (so you’ll see invalid months)

day ∈ 0..35 (so you’ll see invalid days)

What it does:

For each generated (y,m,d), formats "YYYY-MM-DD", computes the same expected as in #1, and then:

arduino
Copy
Edit
if parse_date == expected { println!("[pass] …"); }  
else                     { println!("[FAIL] … got X, expected Y"); }  
Never panics, so you see all passes and all fails in one run.

Why: gives a full 20-line audit trail showing which combinations your parser accepts and which it rejects.

Together, these nine tests give you:

Automatic, broad coverage (tests 1–3).

Concrete edge-case guarantees (tests 4–8).

A full audit demonstration (test 9).